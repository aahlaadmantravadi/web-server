{
	"name": "my-web-server",
	"version": "1.0.0",
	"description": "A stable web server built from scratch in Node.js, based on the book.",
	"main": "dist/server.js",
	"scripts": {
	  "start": "node dist/server.js",
	  "build": "tsc",
	  "test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
	  "@types/node": "^20.12.7",
	  "typescript": "^5.4.5"
	}
  }